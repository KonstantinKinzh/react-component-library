import "./Loader.css";

export const Loader = ({ width, height }) => {
  return (
    <span
      style={{
        width: width,
        height: height,
      }}
      class="loader"
    />
  );
};
